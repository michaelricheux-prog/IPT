Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\venv\Scripts\activate
uvicorn backend.main:app --reload 
git pull
git branch
git fetch --all
git fetch --all --prune
bash
git pull --all
git clone https://github.com/votre-nom/votre-projet.git
git stash
git pull origin feature/core-localv2
git add .gitignore
git add .
git commit -m "Fix: ignore pycache and cleanup tracking"
git push origin feature/core-localv2
git checkout feature/core-localv2

git add . 
git commit -m "sauvegarde"
git push origin feature/core-localv2

git status

git diff

git checkout feature/core-localv2
git pull origin feature/core-localv2
git checkout -b feature/core-localv3
git push -u origin feature/core-localv3